import React from 'react'
import ReactDOM from 'react-dom/client'
import FleetApp from './app/FleetApp'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FleetApp />
  </React.StrictMode>,
)
